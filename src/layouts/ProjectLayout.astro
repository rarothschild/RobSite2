---
export interface Props {
	title: string;
}

const { title } = Astro.props as Props;
---

<div class="max-w-5xl border-black border">
    <div class="w-[65rem] p-2">
        <div class="text-lg font-bold">{title}</div>
        <slot />
    </div>
</div>